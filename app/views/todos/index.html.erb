<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <link href="/app/assets/bootstrap.min.css" rel="stylesheet">
  </head>
  <body>
    <div class="container">
      <div class="title">
        <div class="title_w">
          PureBread Order List
        </div>
        <div class="order">
          <span class="a"><%= link_to 'New Order', new_todo_path %></span>
        </div>
      </div>
      <div class="list">
      <table class="table table-striped table-condensed ">
        <%@todos.each do |todo|%>
          <tr class="word2">
            <td>
              <span class="glyphicon glyphicon-ok">&#160</span>
              <%=link_to todo.name ,todo_path(todo)%>
              <span>&#160&#160&#160</span>
              <%=todo.date%>
              <span>&#160&#160&#160</span>
              <span class="dat">
                <% if (todo.date < Date.today)%>
                  expiry
                <%else%>
                  <span class="edi"><%=link_to 'Delete',todo_path(todo),method: :delete,data:{confirm: "Are you sure?"} %></span>
                <%end%>
              </span>
              <span class="edi"><%=link_to 'Edit' ,edit_todo_path(todo)%></span>

            </td>
            <td><%link_to 'Show' ,todo_path(todo)%></td>
            <td><%link_to 'Edit' ,edit_todo_path(todo)%></td>
            <td><%link_to 'Delete',todo_path(todo),method: :delete,data:{confirm: "Are you sure?"} %></td>
          </tr>
          <%end%>
        </table>


      </div>
    </div>
  </body>
</html>
